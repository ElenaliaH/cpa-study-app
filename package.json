{
  "name": "cpa-study-app",
  "version": "1.0.0",
  "scripts": {
    "build:supabase": "esbuild tools/supabase-shim.js --bundle --format=iife --outfile=js/vendor/supabase.js"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.110.1",
    "esbuild": "^0.28.1"
  }
}
